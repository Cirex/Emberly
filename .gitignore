# Dependencies
node_modules/
.pnp.*

# Bun — bun.lock is committed for reproducible installs; ignore only local caches
.bun/

# Builds / caches
.turbo/
dist/
build/
.next/
out/
*.tsbuildinfo

# Expo / EAS
.expo/
.expo-shared/
# ios/ is a committed bare-workflow native project — track it, ignore only build output.
apps/resident/ios/Pods/
apps/resident/ios/build/
apps/security/ios/Pods/
apps/security/ios/build/
apps/maintenance/ios/Pods/
apps/maintenance/ios/build/
# Machine-local node path shim written by `pod install`.
.xcode.env.local
# android/ is regenerated by prebuild (was gitignored upstream).
apps/resident/android/

# Vendored local agent tooling that leaked in from the old web repo
.codex/
__pycache__/
*.pyc

# Env & secrets
.env
.env.*
!.env.example
!.env.*.example

# Editor / OS
.DS_Store
*.log

# Test coverage
coverage/

# Vercel (legacy — web deploys via Coolify)
.vercel/

# Local clone of the GitHub wiki repo (published from docs/, not part of this repo)
Emberly.wiki/

# ResMan login debug harness output (raw HTML — may contain session tokens)
.resman-debug/
