import { PlaceholderBoard } from "@/components/ui/PlaceholderBoard";

/** Money — delinquency and balances. Wave-2 replaces this body. */
export default function DelinquencyScreen() {
  return <PlaceholderBoard titleKey="delinquency.title" icon="cash-outline" />;
}
