import { PlaceholderBoard } from "@/components/ui/PlaceholderBoard";

/** Today — the manager's morning board. Wave-2 replaces this body. */
export default function TodayScreen() {
  return <PlaceholderBoard titleKey="today.title" icon="sunny-outline" />;
}
