import { PlaceholderBoard } from "@/components/ui/PlaceholderBoard";

/** Leasing — availability, applications, renewals. Wave-2 replaces this body. */
export default function LeasingScreen() {
  return <PlaceholderBoard titleKey="leasing.title" icon="key-outline" />;
}
