import { PlaceholderBoard } from "@/components/ui/PlaceholderBoard";

/** Property map — the Skia site map. Wave-2 replaces this body. */
export default function PropertyMapScreen() {
  return <PlaceholderBoard titleKey="map.title" icon="map-outline" />;
}
