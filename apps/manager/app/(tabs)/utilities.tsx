import { PlaceholderBoard } from "@/components/ui/PlaceholderBoard";

/** Utilities — consumption and billing. Wave-2 replaces this body. */
export default function UtilitiesScreen() {
  return <PlaceholderBoard titleKey="utilities.title" icon="water-outline" />;
}
