/** Delinquency ("Money") board copy. OWNED by the Delinquency feature agent. */
const en = {
  delinquency: {
    title: "Money",
  },
};

/** Spanish catalog — mirrors `en` key-for-key. */
const es: typeof en = {
  delinquency: {
    title: "Dinero",
  },
};

export const delinquency = { en, es };
