# Database

> **Generated by `bun run docs:schema`. Do not edit by hand.** Structure is read from
> a database built from `apps/web/lib/supabase/schema.sql`; prose is the comment block
> above each `create table` in that same file. To change this document, change the
> schema or its comments.

53 tables · 751 columns · **32 declared foreign keys** · 18 inferred references

## How to read the relationships

This is the part that will bite you, so it comes first.

Only **32** relationships across 53 tables are declared foreign keys. The rest are
**convention**: a column holding another table's id, with nothing in the database
enforcing it. Both join identically in a query. They differ in every other way:

| | declared FK | convention only |
| --- | --- | --- |
| Join works | yes | yes |
| Bad id rejected on insert | yes | **no** |
| Parent delete blocked or cascaded | yes | **no — orphans** |
| PostgREST/Supabase embed (`select=*,other(*)`) | yes | **no** |
| Visible to schema tooling | yes | **no** |

The MCP server's `related` filter is built on declared FKs, which is why some
resource pairs cannot be joined there even though the columns obviously correspond —
`units → transactions` is the standing example.

Below, **→ FK** is enforced and **→ ref** is convention. An inferred reference means
the column carries the exact name of another table's single-column primary key; it is
a strong signal, not a guarantee.

### Columns that hold more than one kind of id

Neither kind of reference describes these — what they point at depends on a sibling
column, so no constraint and no inference can cover them.

- **`access_tokens.subject_id`** — discriminated by `subject_type`. `admin_user` → `admin_users.id`; `scanner` → `scanner_devices.scanner_id`
  - ⚠️ In production TWO active rows hold the ResMan *username* here instead of the uuid. The column is plain text with nothing enforcing either shape, so a username rename silently orphans a live bearer token that keeps authenticating.
- **`access_token_audit_log.subject_id`** — discriminated by `subject_type`. as `access_tokens.subject_id`
- **`admin_alerts.subject_id`** — discriminated by `subject_type`. varies by alert kind
- **`admin_audit_logs.admin_user_id`** — `admin_users.id`, as text

## Contents

**ResMan mirror** — [`resman_buildings`](#resmanbuildings), [`resman_floorplans`](#resmanfloorplans), [`resman_lease_addresses`](#resmanleaseaddresses), [`resman_lease_alternate_contacts`](#resmanleasealternatecontacts), [`resman_lease_employment`](#resmanleaseemployment), [`resman_lease_insurance`](#resmanleaseinsurance), [`resman_lease_vehicles`](#resmanleasevehicles), [`resman_leases`](#resmanleases), [`resman_properties`](#resmanproperties), [`resman_residents`](#resmanresidents), [`resman_sync_runs`](#resmansyncruns), [`resman_sync_state`](#resmansyncstate), [`resman_transactions`](#resmantransactions), [`resman_units`](#resmanunits), [`resman_work_orders`](#resmanworkorders)

**Utilities (MLGW)** — [`mlgw_accounts`](#mlgwaccounts), [`mlgw_bills`](#mlgwbills), [`mlgw_exception_reviews`](#mlgwexceptionreviews), [`mlgw_payments`](#mlgwpayments)

**Property map** — [`map_annotation_audit_logs`](#mapannotationauditlogs), [`map_annotation_photos`](#mapannotationphotos), [`map_annotations`](#mapannotations), [`map_cameras`](#mapcameras)

**Guest passes** — [`guest_pass_bans`](#guestpassbans), [`guest_pass_unit_bans`](#guestpassunitbans), [`guest_passes`](#guestpasses)

**Admin & audit** — [`admin_alerts`](#adminalerts), [`admin_audit_logs`](#adminauditlogs), [`admin_users`](#adminusers)

**Bearer tokens** — [`access_token_audit_log`](#accesstokenauditlog), [`access_token_changes`](#accesstokenchanges), [`access_tokens`](#accesstokens)

**Preventive maintenance** — [`pm_tasks`](#pmtasks), [`pm_templates`](#pmtemplates)

**Gate entry** — [`entry_log_photos`](#entrylogphotos), [`entry_logs`](#entrylogs)

**Residents** — [`resident_devices`](#residentdevices), [`resident_entry_token_uses`](#residententrytokenuses), [`residents`](#residents)

**Everything else** — [`delinquency_actions`](#delinquencyactions), [`insurance_actions`](#insuranceactions), [`lease_notes`](#leasenotes), [`manager_alert_notifications`](#manageralertnotifications), [`monitor_findings`](#monitorfindings), [`property_snapshots`](#propertysnapshots), [`push_tokens`](#pushtokens), [`rate_limits`](#ratelimits), [`renewal_offers`](#renewaloffers), [`scanner_devices`](#scannerdevices), [`unit_snapshots`](#unitsnapshots), [`unit_tags`](#unittags), [`work_order_photos`](#workorderphotos), [`work_order_translations`](#workordertranslations)

## ResMan mirror

Read-only copies of the property-management system, rewritten in full by the sync worker. Nothing here is authored by Emberly — a write would be overwritten on the next pass.

### `resman_buildings`

resman_buildings (from Building)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_building_id` | text | no | **PK** |  |  |
| `resman_property_id` | text | no |  | → FK [`resman_properties.resman_property_id`](#resmanproperties) |  |
| `name` | text | no |  |  | default `''` |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`resman_units.resman_building_id`](#resmanunits) (FK), [`unit_snapshots.resman_building_id`](#unitsnapshots)

### `resman_floorplans`

resman_floorplans (from Floorplan; ResMan UnitTypes)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_floorplan_id` | text | no | **PK** |  |  |
| `resman_property_id` | text | yes |  | → FK [`resman_properties.resman_property_id`](#resmanproperties) |  |
| `name` | text | no |  |  | default `''` |
| `description` | text | no |  |  | default `''` |
| `square_feet` | integer | yes |  |  |  |
| `market_rent` | numeric | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`resman_units.resman_floorplan_id`](#resmanunits) (FK), [`unit_snapshots.resman_floorplan_id`](#unitsnapshots)

### `resman_lease_addresses`

resman_lease_addresses (from LeaseAddress)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_address_id` | text | no | **PK** |  |  |
| `resman_person_lease_id` | text | no |  | → FK [`resman_residents.resman_person_lease_id`](#resmanresidents) |  |
| `address_type` | text | no |  |  | default `''` |
| `street` | text | no |  |  | default `''` |
| `city` | text | no |  |  | default `''` |
| `state` | text | no |  |  | default `''` |
| `postal_code` | text | no |  |  | default `''` |
| `country` | text | no |  |  | default `''` |
| `start_date` | date | yes |  |  | always empty: the Addresses tab records no dates |
| `end_date` | date | yes |  |  | always empty: the Addresses tab records no dates |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_lease_alternate_contacts`

resman_lease_alternate_contacts (from LeaseAlternateContact)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_contact_id` | text | no | **PK** |  |  |
| `resman_person_lease_id` | text | no |  | → FK [`resman_residents.resman_person_lease_id`](#resmanresidents) |  |
| `name` | text | no |  |  | default `''` |
| `relationship` | text | no |  |  | default `''` |
| `phone` | text | no |  |  | default `''` |
| `email` | text | no |  |  | default `''` |
| `is_emergency_contact` | boolean | no |  |  | default `false` |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_lease_employment`

resman_lease_employment (from LeaseEmployment)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_employment_id` | text | no | **PK** |  |  |
| `resman_person_lease_id` | text | no |  | → FK [`resman_residents.resman_person_lease_id`](#resmanresidents) |  |
| `employer_name` | text | no |  |  | default `''` |
| `position` | text | no |  |  | default `''` |
| `phone` | text | no |  |  | default `''` |
| `other_income_source` | text | no |  |  | other-income records only (Industry cell reads "Other Income"); empty on a job · default `''` |
| `monthly_income` | numeric | yes |  |  | MONTHLY, normalized from the tab's pay period |
| `other_income` | numeric | yes |  |  | MONTHLY, normalized from the tab's pay period |
| `start_date` | date | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_lease_insurance`

resman_lease_insurance (from LeaseInsurance)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_insurance_id` | text | no | **PK** |  |  |
| `resman_person_lease_id` | text | no |  | → FK [`resman_residents.resman_person_lease_id`](#resmanresidents) |  |
| `provider` | text | no |  |  | default `''` |
| `policy_number` | text | no |  |  | default `''` |
| `policy_type` | text | no |  |  | default `''` |
| `status` | text | no |  |  | default `''` |
| `start_date` | date | yes |  |  | always empty: the Insurance tab records only an expiration date |
| `end_date` | date | yes |  |  |  |
| `coverage_amount` | numeric | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_lease_vehicles`

resman_lease_vehicles (from LeaseVehicle)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_vehicle_id` | text | no | **PK** |  |  |
| `resman_person_lease_id` | text | no |  | → FK [`resman_residents.resman_person_lease_id`](#resmanresidents) |  |
| `make` | text | no |  |  | default `''` |
| `model` | text | no |  |  | default `''` |
| `year` | text | no |  |  | default `''` |
| `color` | text | no |  |  | default `''` |
| `license_plate` | text | no |  |  | default `''` |
| `license_plate_state` | text | no |  |  | default `''` |
| `parking_spot` | text | no |  |  | always empty: ResMan has no parking-space field; see permit_number · default `''` |
| `permit_number` | text | no |  |  | parking decal number ("Permit number" on the Vehicles tab) · default `''` |
| `notes` | text | no |  |  | free text on the Vehicles tab, usually decal history · default `''` |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_leases`

resman_leases (from Lease)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_lease_id` | text | no | **PK** |  |  |
| `unit_lease_group_id` | text | no |  |  | <=> residents.resman_ledger_id · default `''` |
| `resman_property_id` | text | yes |  | → FK [`resman_properties.resman_property_id`](#resmanproperties) |  |
| `resman_unit_id` | text | yes |  | → FK [`resman_units.resman_unit_id`](#resmanunits) |  |
| `unit_number` | text | no |  |  | default `''` |
| `status` | text | no |  |  | raw ResMan status string · default `''` |
| `approval_status` | text | no |  |  | default `''` |
| `approved_date` | date | yes |  |  | from the Activity Log; ResMan has no approval-date field |
| `approved_by` | text | no |  |  | the staff member the Activity Log credits · default `''` |
| `application_date` | date | yes |  |  |  |
| `signed_date` | date | yes |  |  |  |
| `start_date` | date | yes |  |  |  |
| `end_date` | date | yes |  |  |  |
| `move_in_date` | date | yes |  |  |  |
| `move_out_date` | date | yes |  |  |  |
| `leasing_agent` | text | no |  |  | default `''` |
| `renewal_date` | date | yes |  |  |  |
| `notice_given_date` | date | yes |  |  |  |
| `market_rent` | numeric | yes |  |  |  |
| `resident_rent` | numeric | yes |  |  |  |
| `hap_rent` | numeric | yes |  |  |  |
| `monthly_charge` | numeric | yes |  |  |  |
| `balance` | numeric | yes |  |  |  |
| `collection_balance` | numeric | yes |  |  |  |
| `reason_for_leaving` | text | no |  |  | default `''` |
| `is_current_lease` | boolean | no |  |  | default `false` |
| `is_most_recent_lease` | boolean | no |  |  | default `false` |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `raw` | jsonb | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |
| `deep_synced_at` | timestamptz | yes |  |  |  |

**Referenced by** — [`resman_residents.resman_lease_id`](#resmanresidents) (FK), [`resman_transactions.resman_lease_id`](#resmantransactions) (FK), [`delinquency_actions.resman_lease_id`](#delinquencyactions), [`insurance_actions.resman_lease_id`](#insuranceactions), [`lease_notes.resman_lease_id`](#leasenotes), [`renewal_offers.resman_lease_id`](#renewaloffers), [`resman_work_orders.resman_lease_id`](#resmanworkorders)

### `resman_properties`

ResMan (resman_*) + MLGW (mlgw_*) sync-mirror schema
Ported from the Swift "Kraken" package (docs/resman-port-design.md §2).
Natural source-id text primary keys; enums as text + check constraints;
RLS enabled with no policies (service role only); updated_at via the
existing public.update_updated_at_column() trigger. Migration:
lib/supabase/migrations/20260711_resman_mlgw_sync.sql

resman_companies was dropped on 2026-08-02 (deltas/2026-08-02-drop-resman-companies.sql).
It held the subdomain / account / company config for a multi-tenant install.
This deployment serves one ResMan account, and the sync derives those values
from ENV (supabase/sync/src/resman/config.ts) — the table was never read.

resman_properties (from Property)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_property_id` | text | no | **PK** |  |  |
| `resman_account_id` | text | no |  |  | default `'1659'` |
| `name` | text | no |  |  | default `''` |
| `custom_name` | text | no |  |  | user-set; sync must never overwrite · default `''` |
| `abbreviation` | text | no |  |  | default `''` |
| `phone` | text | no |  |  | default `''` |
| `email` | text | no |  |  | default `''` |
| `website` | text | no |  |  | default `''` |
| `logo_url` | text | no |  |  | default `''` |
| `management_company` | text | no |  |  | default `''` |
| `property_type` | text | no |  |  | default `''` |
| `time_zone` | text | no |  |  | default `''` |
| `regional_manager` | text | no |  |  | default `''` |
| `property_manager` | text | no |  |  | default `''` |
| `leasing_agent` | text | no |  |  | default `''` |
| `resident_portal_url` | text | no |  |  | default `''` |
| `address` | text | no |  |  | default `''` |
| `city` | text | no |  |  | default `''` |
| `state` | text | no |  |  | default `''` |
| `postal_code` | text | no |  |  | default `''` |
| `unit_count` | integer | no |  |  | default `0` |
| `last_sync_date` | timestamptz | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `raw` | jsonb | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`resman_buildings.resman_property_id`](#resmanbuildings) (FK), [`resman_floorplans.resman_property_id`](#resmanfloorplans) (FK), [`resman_leases.resman_property_id`](#resmanleases) (FK), [`resman_units.resman_property_id`](#resmanunits) (FK), [`resman_work_orders.resman_property_id`](#resmanworkorders) (FK), [`mlgw_accounts.resman_property_id`](#mlgwaccounts), [`mlgw_bills.resman_property_id`](#mlgwbills), [`mlgw_exception_reviews.resman_property_id`](#mlgwexceptionreviews), [`mlgw_payments.resman_property_id`](#mlgwpayments), [`resman_sync_runs.resman_property_id`](#resmansyncruns), [`resman_transactions.resman_property_id`](#resmantransactions)

### `resman_residents`

resman_residents (from Resident / LeaseResident) — occupant records.
Distinct from the existing public.residents table (portal logins).

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_person_lease_id` | text | no | **PK** |  |  |
| `resman_person_id` | text | no |  |  | default `''` |
| `resman_lease_id` | text | no |  | → FK [`resman_leases.resman_lease_id`](#resmanleases) |  |
| `first_name` | text | no |  |  | default `''` |
| `last_name` | text | no |  |  | default `''` |
| `email` | text | no |  |  | default `''` |
| `phone_numbers` | text[] | no |  |  | default `'{}'[]` |
| `gender` | text | no |  |  | default `''` |
| `birthdate` | date | yes |  |  | PII |
| `household_status` | text | no |  |  | default `''` |
| `drivers_license` | text | no |  |  | PII · default `''` |
| `drivers_license_state` | text | no |  |  | PII · default `''` |
| `language` | text | no |  |  | default `''` |
| `identification` | text | no |  |  | default `''` |
| `is_primary` | boolean | no |  |  | default `false` |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `raw` | jsonb | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`resman_lease_addresses.resman_person_lease_id`](#resmanleaseaddresses) (FK), [`resman_lease_alternate_contacts.resman_person_lease_id`](#resmanleasealternatecontacts) (FK), [`resman_lease_employment.resman_person_lease_id`](#resmanleaseemployment) (FK), [`resman_lease_insurance.resman_person_lease_id`](#resmanleaseinsurance) (FK), [`resman_lease_vehicles.resman_person_lease_id`](#resmanleasevehicles) (FK)

### `resman_sync_runs`

resman_sync_runs (one row per job execution)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `job` | text | no |  |  |  |
| `resman_account_id` | text | yes |  |  |  |
| `resman_property_id` | text | yes |  | → ref [`resman_properties.resman_property_id`](#resmanproperties) |  |
| `status` | text | no |  |  |  |
| `started_at` | timestamptz | no |  |  | default `now()` |
| `finished_at` | timestamptz | yes |  |  |  |
| `rows_upserted` | integer | no |  |  | default `0` |
| `rows_failed` | integer | no |  |  | default `0` |
| `error` | text | yes |  |  |  |
| `metadata` | jsonb | no |  |  | default `'{}'` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`resman_sync_state.last_run_id`](#resmansyncstate) (FK)

**Allowed values**

- `status` — `running`, `succeeded`, `failed`, `partial`, `skipped`

### `resman_sync_state`

resman_sync_state (per-(job, property) incremental watermark)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `job` | text | no | **PK** |  |  |
| `resman_property_id` | text | no | **PK** |  | default `''` |
| `last_synced_at` | timestamptz | yes |  |  |  |
| `last_run_id` | uuid | yes |  | → FK [`resman_sync_runs.id`](#resmansyncruns) |  |
| `cursor` | jsonb | no |  |  | default `'{}'` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_transactions`

resman_transactions (from Transaction; ledger)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_ledger_entry_id` | text | no | **PK** |  |  |
| `resman_property_id` | text | no |  | → ref [`resman_properties.resman_property_id`](#resmanproperties) | soft · default `''` |
| `resman_unit_id` | text | no |  | → ref [`resman_units.resman_unit_id`](#resmanunits) | soft · default `''` |
| `resman_lease_id` | text | yes |  | → FK [`resman_leases.resman_lease_id`](#resmanleases) |  |
| `transaction_id` | text | no |  |  | ResMan real UUID · default `''` |
| `transaction_type` | text | no |  |  | default `''` |
| `date` | date | yes |  |  |  |
| `reference` | text | no |  |  | default `''` |
| `batch` | text | no |  |  | default `''` |
| `batch_id` | text | no |  |  | default `''` |
| `category` | text | no |  |  | default `''` |
| `ledger_description` | text | no |  |  | default `''` |
| `notes` | text | no |  |  | default `''` |
| `charges` | numeric | yes |  |  |  |
| `credits` | numeric | yes |  |  |  |
| `balance` | numeric | yes |  |  |  |
| `ledger_sequence` | integer | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resman_units`

resman_units (from PropertyUnit)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_unit_id` | text | no | **PK** |  |  |
| `resman_property_id` | text | no |  | → FK [`resman_properties.resman_property_id`](#resmanproperties) |  |
| `resman_building_id` | text | yes |  | → FK [`resman_buildings.resman_building_id`](#resmanbuildings) |  |
| `resman_floorplan_id` | text | yes |  | → FK [`resman_floorplans.resman_floorplan_id`](#resmanfloorplans) | from the unit PAGE (sync:unit-details), not the roster report |
| `number` | text | no |  |  | default `''` |
| `current_lease_id` | text | yes |  |  | denormalized, nullable (no FK) |
| `pending_lease_id` | text | yes |  |  | denormalized, nullable |
| `availability` | text | no |  |  | default `''` |
| `lease_status` | text | yes |  |  |  |
| `occupancy_status` | text | yes |  |  |  |
| `classification` | text | no |  |  | default `''` |
| `notes` | text | no |  |  | default `''` |
| `occupied` | boolean | yes |  |  |  |
| `market_rent` | numeric | yes |  |  |  |
| `lease_rent` | numeric | yes |  |  |  |
| `deposit_required` | numeric | yes |  |  |  |
| `deposit_held` | numeric | yes |  |  |  |
| `balance` | numeric | yes |  |  |  |
| `bedrooms` | integer | yes |  |  |  |
| `bathrooms` | numeric | yes |  |  |  |
| `pets_permitted` | boolean | yes |  |  | from the unit PAGE (sync:unit-details) |
| `affordable_unit` | boolean | yes |  |  | from the unit PAGE (sync:unit-details) |
| `holding_unit` | boolean | yes |  |  |  |
| `excluded_from_occupancy` | boolean | yes |  |  |  |
| `available_for_online_marketing` | boolean | yes |  |  |  |
| `street` | text | no |  |  | default `''` |
| `city` | text | no |  |  | default `''` |
| `state` | text | no |  |  | default `''` |
| `postal_code` | text | no |  |  | default `''` |
| `country` | text | no |  |  | from the unit PAGE (sync:unit-details); the unit-info report has no country · default `''` |
| `lease_start_date` | date | yes |  |  |  |
| `lease_end_date` | date | yes |  |  |  |
| `move_in_date` | date | yes |  |  |  |
| `move_out_date` | date | yes |  |  |  |
| `lease_term` | text | yes |  |  |  |
| `old_lease_id` | text | yes |  |  |  |
| `date_available` | date | yes |  |  |  |
| `leasing_agent` | text | yes |  |  |  |
| `floor` | text | yes |  |  |  |
| `hearing_accessible` | boolean | yes |  |  |  |
| `mobility_accessible` | boolean | yes |  |  |  |
| `visual_accessible` | boolean | yes |  |  |  |
| `pending_move_in_date` | date | yes |  |  |  |
| `pending_lease_start_date` | date | yes |  |  |  |
| `pending_lease_end_date` | date | yes |  |  |  |
| `max_occupancy` | integer | yes |  |  |  |
| `current_month_balance` | numeric | yes |  |  |  |
| `last_month_balance` | numeric | yes |  |  |  |
| `period_balance` | numeric | yes |  |  |  |
| `previous_balance` | numeric | yes |  |  |  |
| `times_late` | integer | yes |  |  |  |
| `delinquency_reason` | text | yes |  |  |  |
| `tenant_names` | text[] | no |  |  | default `'{}'[]` |
| `source_url` | text | no |  |  | default `''` |
| `scraped_at` | timestamptz | yes |  |  | detail-scrape freshness (drives incremental sync) |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `raw` | jsonb | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`resman_leases.resman_unit_id`](#resmanleases) (FK), [`resman_work_orders.resman_unit_id`](#resmanworkorders) (FK), [`delinquency_actions.resman_unit_id`](#delinquencyactions), [`mlgw_accounts.resman_unit_id`](#mlgwaccounts), [`renewal_offers.resman_unit_id`](#renewaloffers), [`resman_transactions.resman_unit_id`](#resmantransactions)

**Allowed values**

- `lease_status` — `Current`, `Under Eviction`, `Notice to Vacate`, `Month to Month`, `Pending`, `Pending Renewal`, `Cancelled`
- `occupancy_status` — `Occupied`, `Vacant`, `Notice`

### `resman_work_orders`

resman_work_orders (from WorkOrder)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_work_order_id` | text | no | **PK** |  |  |
| `number` | text | no |  |  | default `''` |
| `resman_unit_id` | text | yes |  | → FK [`resman_units.resman_unit_id`](#resmanunits) |  |
| `unit_lease_group_id` | text | no |  |  | always empty: the work-order report carries no lease link · default `''` |
| `resman_lease_id` | text | no |  | → ref [`resman_leases.resman_lease_id`](#resmanleases) | always empty: the work-order report carries no lease link · default `''` |
| `unit_number` | text | no |  |  | default `''` |
| `resman_property_id` | text | yes |  | → FK [`resman_properties.resman_property_id`](#resmanproperties) |  |
| `status` | text | no |  |  | default `'Not Started'` |
| `priority` | text | no |  |  | default `'Normal'` |
| `category` | text | no |  |  | default `''` |
| `title` | text | no |  |  | default `''` |
| `notes` | text | no |  |  | default `''` |
| `completion_notes` | text | no |  |  | default `''` |
| `technician` | text | no |  |  | default `''` |
| `date_reported` | date | yes |  |  |  |
| `date_scheduled` | date | yes |  |  |  |
| `date_completed` | date | yes |  |  |  |
| `is_make_ready` | boolean | no |  |  | default `false` |
| `callback_requested` | boolean | no |  |  | default `false` |
| `callback_completed` | boolean | no |  |  | default `false` |
| `tags` | text[] | no |  |  | derived (WorkOrderTagging) · default `'{}'[]` |
| `is_duplicate` | boolean | no |  |  | derived · default `false` |
| `callback_status` | text | no |  |  | default `'none'` |
| `callback_matched_work_order_id` | text | no |  |  | default `''` |
| `callback_engine_version` | text | no |  |  | default `''` |
| `callback_source` | text | no |  |  | default `''` |
| `callback_detected_at` | timestamptz | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `raw` | jsonb | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`work_order_photos.resman_work_order_id`](#workorderphotos) (FK), [`pm_tasks.resman_work_order_id`](#pmtasks)

**Allowed values**

- `priority` — `Emergency`, `High`, `Normal`, `Low`
- `status` — `Not Started`, `Scheduled`, `In Progress`, `Completed`, `Closed`, `Canceled`
- `callback_status` — `none`, `possible`, `confirmed`, `dismissed`

## Utilities (MLGW)

Memphis Light Gas & Water billing: accounts, bills, payments, and the exception reviews staff record against them.

### `mlgw_accounts`

mlgw_accounts (from MLGWAccount)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | text | no | **PK** |  |  |
| `resman_property_id` | text | no |  | → ref [`resman_properties.resman_property_id`](#resmanproperties) | soft ref · default `''` |
| `property_name` | text | no |  |  | default `''` |
| `account_number` | text | no |  |  | default `''` |
| `service_address` | text | no |  |  | default `''` |
| `resman_unit_id` | text | no |  | → ref [`resman_units.resman_unit_id`](#resmanunits) | soft ref (heuristic match) · default `''` |
| `unit_number` | text | no |  |  | default `''` |
| `is_house_account` | boolean | no |  |  | default `false` |
| `due_now` | numeric | yes |  |  |  |
| `due_date` | date | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`mlgw_bills.mlgw_account_id`](#mlgwbills) (FK), [`mlgw_payments.mlgw_account_id`](#mlgwpayments) (FK)

### `mlgw_bills`

mlgw_bills (from MLGWBill). file_path -> Supabase Storage object path.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | text | no | **PK** |  |  |
| `document_key` | text | no |  |  | defaults to id if blank · default `''` |
| `mlgw_account_id` | text | yes |  | → FK [`mlgw_accounts.id`](#mlgwaccounts) |  |
| `resman_property_id` | text | no |  | → ref [`resman_properties.resman_property_id`](#resmanproperties) | soft · default `''` |
| `document_id` | text | no |  |  | default `''` |
| `is_current` | boolean | no |  |  | default `true` |
| `bill_date` | date | yes |  |  |  |
| `due_date` | date | yes |  |  |  |
| `amount_due` | numeric | yes |  |  |  |
| `balance_forward` | numeric | yes |  |  |  |
| `average_temperature` | numeric | yes |  |  |  |
| `bill_for` | text | no |  |  | default `''` |
| `file_path` | text | no |  |  | Supabase Storage object path (mlgw-bills bucket) · default `''` |
| `gas_usage` | text | no |  |  | default `''` |
| `gas_read_start_date` | date | yes |  |  |  |
| `gas_read_end_date` | date | yes |  |  |  |
| `gas_total` | numeric | yes |  |  |  |
| `electric_usage` | text | no |  |  | default `''` |
| `electric_read_start_date` | date | yes |  |  |  |
| `electric_read_end_date` | date | yes |  |  |  |
| `electric_total` | numeric | yes |  |  |  |
| `water_usage` | text | no |  |  | default `''` |
| `water_read_start_date` | date | yes |  |  |  |
| `water_read_end_date` | date | yes |  |  |  |
| `water_total` | numeric | yes |  |  |  |
| `sewer_usage` | text | no |  |  | default `''` |
| `sewer_read_start_date` | date | yes |  |  |  |
| `sewer_read_end_date` | date | yes |  |  |  |
| `sewer_total` | numeric | yes |  |  |  |
| `other_mlgw_total` | numeric | yes |  |  |  |
| `non_mlgw_total` | numeric | yes |  |  |  |
| `street_light_fee_total` | numeric | yes |  |  |  |
| `electrical_late_fee_total` | numeric | yes |  |  |  |
| `security_deposit_total` | numeric | yes |  |  |  |
| `smart_meter_connect_charge_total` | numeric | yes |  |  |  |
| `credit_balance_transfer_total` | numeric | yes |  |  |  |
| `share_the_pennies_total` | numeric | yes |  |  |  |
| `water_cross_connection_fee_total` | numeric | yes |  |  |  |
| `leasing_outdoor_lighting_total` | numeric | yes |  |  |  |
| `mosquito_rodent_control_fee_total` | numeric | yes |  |  |  |
| `sewer_charge_total` | numeric | yes |  |  |  |
| `storm_water_fee_total` | numeric | yes |  |  |  |
| `solid_waste_fee_total` | numeric | yes |  |  |  |
| `synced_at` | timestamptz | yes |  |  | default `now()` |
| `raw` | jsonb | yes |  |  | parsed bill DTO + charge array |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `mlgw_exception_reviews`

mlgw_exception_reviews (from UtilityExceptionReview)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | text | no | **PK** |  | propertyId|billId|exceptionKind |
| `resman_property_id` | text | no |  | → ref [`resman_properties.resman_property_id`](#resmanproperties) | default `''` |
| `bill_id` | text | no |  |  | soft ref to mlgw_bills.id · default `''` |
| `account_number` | text | no |  |  | default `''` |
| `exception_kind` | text | no |  |  | default `''` |
| `reviewed_at` | timestamptz | yes |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

### `mlgw_payments`

mlgw_payments (from MLGWPayment). Card fields intentionally dropped
(confirmed 2026-07-11): smallest PII surface — keep amount, date,
confirmation, method only; no masked_card_number / name_on_card.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | text | no | **PK** |  | propertyId|payment|<digits> |
| `mlgw_account_id` | text | yes |  | → FK [`mlgw_accounts.id`](#mlgwaccounts) |  |
| `resman_property_id` | text | no |  | → ref [`resman_properties.resman_property_id`](#resmanproperties) | soft · default `''` |
| `account_number` | text | no |  |  | default `''` |
| `reference_number` | text | no |  |  | default `''` |
| `status` | text | no |  |  | default `''` |
| `amount` | numeric | yes |  |  |  |
| `paid_date` | date | yes |  |  |  |
| `payment_method` | text | no |  |  | default `''` |
| `authorization_number` | text | no |  |  | default `''` |
| `account_selection` | text | no |  |  | default `''` |
| `fetched_at` | timestamptz | yes |  |  | default `now()` |
| `detail_fetched_at` | timestamptz | yes |  |  |  |
| `detail_text` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

## Property map

The annotation layer the security app draws on, plus the key exchange that lets a device sync it.

### `map_annotation_audit_logs`

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_account_id` | text | no |  | → FK [`map_annotations.resman_account_id`](#mapannotations) |  |
| `property_id` | text | no |  | → FK [`map_annotations.property_id`](#mapannotations) |  |
| `feature_key` | text | no |  | → FK [`map_annotations.feature_key`](#mapannotations) |  |
| `action` | text | no |  |  |  |
| `annotation_id` | uuid | yes |  | → FK [`map_annotations.id`](#mapannotations) |  |
| `actor_display_name` | text | yes |  |  |  |
| `admin_user_id` | text | yes |  |  |  |
| `admin_display_name` | text | yes |  |  |  |
| `metadata` | jsonb | no |  |  | default `'{}'` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `action` — `access.request`, `access.approve`, `access.reject`, `access.claim`, `access.revoke`, `annotation.create`, `annotation.update`, `annotation.delete`

### `map_annotation_photos`

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `annotation_id` | uuid | no |  | → FK [`map_annotations.id`](#mapannotations) |  |
| `resman_account_id` | text | no |  | → FK [`map_annotations.resman_account_id`](#mapannotations) |  |
| `property_id` | text | no |  | → FK [`map_annotations.property_id`](#mapannotations) |  |
| `feature_key` | text | no |  | → FK [`map_annotations.feature_key`](#mapannotations) | default `'property_map.annotations'` |
| `storage_path` | text | no |  |  |  |
| `content_type` | text | no |  |  |  |
| `byte_size` | integer | no |  |  |  |
| `created_by` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_at` | timestamptz | yes |  |  |  |

### `map_annotations`

map_sync_access_requests and map_sync_keys were dropped on 2026-08-02
(deltas/2026-08-02-drop-map-sync-subsystem.sql). They backed a device
enrolment handshake for an external sync client that was never built; no
client in this repo could create the first row, and both held zero rows.
Annotations are written through /api/admin/map-annotations, which accepts a
maintenance staff token or a security scanner key.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_account_id` | text | no |  |  |  |
| `property_id` | text | no |  |  |  |
| `feature_key` | text | no |  |  | default `'property_map.annotations'` |
| `title` | text | no |  |  |  |
| `notes` | text | no |  |  | default `''` |
| `normalized_x` | float8 | no |  |  |  |
| `normalized_y` | float8 | no |  |  |  |
| `color_hex` | text | no |  |  |  |
| `layer` | text | no |  |  | default `'staff'` |
| `kind` | text | no |  |  | default `'pin'` |
| `utility_type` | text | yes |  |  |  |
| `points` | jsonb | yes |  |  |  |
| `line_style` | text | yes |  |  |  |
| `line_weight` | text | yes |  |  |  |
| `flow_arrows` | boolean | yes |  |  |  |
| `origin` | text | no |  |  | default `'sync'` |
| `created_by_display_name` | text | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_by_display_name` | text | yes |  |  |  |
| `updated_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_by_display_name` | text | yes |  |  |  |
| `deleted_at` | timestamptz | yes |  |  |  |
| `version` | integer | no |  |  | default `1` |
| `icon` | text | no |  |  | default `'document-text'` |

**Referenced by** — [`map_annotation_audit_logs.annotation_id`](#mapannotationauditlogs) (FK), [`map_annotation_audit_logs.feature_key`](#mapannotationauditlogs) (FK), [`map_annotation_audit_logs.property_id`](#mapannotationauditlogs) (FK), [`map_annotation_audit_logs.resman_account_id`](#mapannotationauditlogs) (FK), [`map_annotation_photos.annotation_id`](#mapannotationphotos) (FK), [`map_annotation_photos.feature_key`](#mapannotationphotos) (FK), [`map_annotation_photos.property_id`](#mapannotationphotos) (FK), [`map_annotation_photos.resman_account_id`](#mapannotationphotos) (FK)

**Allowed values**

- `origin` — `sync`, `admin`, `scanner`
- `line_weight` — `thin`, `medium`, `thick`
- `line_style` — `solid`, `dashed`, `dotted`
- `utility_type` — `water`, `sewer`, `gas`, `electrical`, `internet`, `other`
- `kind` — `pin`, `utility_pin`, `utility_line`
- `layer` — `staff`, `security`, `utility`

### `map_cameras`

Security-camera markers on the property map. Placed and edited only in the
admin portal; the guard iPads read them (coverage cones on the map) but can
never move, add, or change one — enforced by the API, this table just
stores the truth.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `normalized_x` | float8 | no |  |  |  |
| `normalized_y` | float8 | no |  |  |  |
| `direction` | float8 | no |  |  | default `0` |
| `fov` | float8 | no |  |  | default `70` |
| `range` | float8 | no |  |  | default `0.06` |
| `active` | boolean | no |  |  | default `true` |
| `unifi_console_id` | text | yes |  |  |  |
| `unifi_camera_id` | text | yes |  |  |  |
| `unifi_camera_name` | text | yes |  |  |  |
| `unifi_camera_name_synced_at` | timestamptz | yes |  |  |  |
| `created_by_display_name` | text | yes |  |  |  |
| `updated_by_display_name` | text | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

## Guest passes

Resident-issued visitor passes and the bans that suspend them.

### `guest_pass_bans`

Admin: residents suspended from creating guest passes

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resident_id` | uuid | no | UQ | → FK [`residents.id`](#residents) |  |
| `reason` | text | yes |  |  |  |
| `banned_by` | text | no |  |  |  |
| `banned_at` | timestamptz | yes |  |  | default `now()` |

### `guest_pass_unit_bans`

Admin: units suspended from guest visits (no enrollment needed)

Keys the suspension to the ResMan unit so households that never registered
a resident login can still have guest visits disabled. unit_number is the
bridge to the first-party side (residents.unit_id / entry_logs), which is
where creation and verify-pass enforce it.
Expiry follows the unit-tag vocabulary (see lib/unit-expiry.ts), so a
suspension can lift at move-out the way the tag that prompted it does,
instead of outliving the household it was set against.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `resman_unit_id` | text | no | **PK** |  |  |
| `unit_number` | text | no |  |  |  |
| `reason` | text | yes |  |  |  |
| `banned_by` | text | no |  |  |  |
| `banned_at` | timestamptz | no |  |  | default `now()` |
| `expiry_kind` | text | no |  |  | default `'never'` |
| `expires_at` | timestamptz | yes |  |  |  |
| `bound_lease_id` | text | yes |  |  |  |
| `status_trigger` | text | yes |  |  |  |

**Allowed values**

- `expiry_kind` — `never`, `date`, `duration`, `move_out`, `status_change`

### `guest_passes`

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resident_id` | uuid | no |  | → FK [`residents.id`](#residents) |  |
| `guest_name` | text | no |  |  |  |
| `guest_email` | text | no |  |  |  |
| `guest_phone` | text | yes |  |  |  |
| `guest_address` | text | yes |  |  |  |
| `share_token` | text | no | UQ |  | random token used for public browser share URLs |
| `expires_at` | timestamptz | no |  |  | 24h from creation |
| `used_at` | timestamptz | yes |  |  |  |
| `status` | text | no |  |  | default `'active'` |
| `email_delivery_status` | text | no |  |  | default `'pending'` |
| `email_provider_id` | text | yes |  |  |  |
| `email_sent_at` | timestamptz | yes |  |  |  |
| `email_last_error` | text | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`entry_log_photos.guest_pass_id`](#entrylogphotos) (FK), [`entry_logs.guest_pass_id`](#entrylogs) (FK)

**Allowed values**

- `status` — `active`, `revoked`, `used`
- `email_delivery_status` — `pending`, `sent`, `failed`

## Admin & audit

Staff accounts, the alert feed, and the audit trails.

### `admin_alerts`

Admin: exception alerts

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `alert_type` | text | no |  |  |  |
| `severity` | text | no |  |  |  |
| `subject_type` | text | no |  |  |  |
| `subject_id` | text | no |  | → _polymorphic, see above_ |  |
| `title` | text | no |  |  |  |
| `detail` | text | yes |  |  |  |
| `metadata` | jsonb | no |  |  | default `'{}'` |
| `status` | text | no |  |  | default `'open'` |
| `resolved_at` | timestamptz | yes |  |  |  |
| `resolved_by` | text | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `severity` — `info`, `warning`, `critical`
- `subject_type` — `resident`, `guest_pass`, `scanner`, `system`
- `status` — `open`, `resolved`
- `alert_type` — `resident_access_stale`, `resident_access_denied`, `scanner_offline`, `guest_pass_denied`, `security_scan_denied`

### `admin_audit_logs`

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `admin_user_id` | text | no |  | → _polymorphic, see above_ |  |
| `admin_role` | text | no |  |  |  |
| `admin_display_name` | text | yes |  |  |  |
| `action` | text | no |  |  |  |
| `target_type` | text | no |  |  |  |
| `target_id` | text | no |  |  |  |
| `metadata` | jsonb | no |  |  | default `'{}'` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

### `admin_users`

Admin users and audit logs

Admin users are authenticated against the ResMan staff portal; a row is created
on first successful login. The retired shared ADMIN_LOGIN_KEY model left a
key_hash column behind; it was dropped on 2026-08-02.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `email` | text | yes | UQ |  |  |
| `display_name` | text | yes |  |  |  |
| `role` | text | no |  |  |  |
| `resman_username` | text | yes |  |  |  |
| `resman_person_id` | text | yes |  |  |  |
| `last_login_at` | timestamptz | yes |  |  |  |
| `active` | boolean | no |  |  | default `true` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `role` — `super_admin`, `property_manager`, `security_manager`, `viewer`

## Bearer tokens

Hash-only storage for MCP and API tokens, their use log, and their permission-change trail.

### `access_token_audit_log`

Per-call attribution for access_tokens (MCP tool calls / API routes).

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `token_id` | uuid | yes |  | → FK [`access_tokens.id`](#accesstokens) |  |
| `subject_type` | text | no |  |  | default `''` |
| `subject_id` | text | no |  | → _polymorphic, see above_ | default `''` |
| `label` | text | no |  |  | default `''` |
| `kind` | text | no |  |  | default `''` |
| `tool` | text | no |  |  | MCP tool or API route · default `''` |
| `resource` | text | no |  |  | default `''` |
| `arguments` | jsonb | yes |  |  |  |
| `ok` | boolean | no |  |  | default `true` |
| `error` | text | no |  |  | default `''` |
| `created_at` | timestamptz | no |  |  | default `now()` |

### `access_token_changes`

access_token_changes — a trail for PERMISSION changes

access_token_audit_log records what a token DID; this records what was done
TO it. See the trigger below for why it is enforced in the database.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `token_id` | uuid | no |  |  |  |
| `label` | text | yes |  |  |  |
| `kind` | text | yes |  |  |  |
| `action` | text | no |  |  |  |
| `scopes_before` | jsonb | yes |  |  |  |
| `scopes_after` | jsonb | yes |  |  |  |
| `active_before` | boolean | yes |  |  |  |
| `active_after` | boolean | yes |  |  |  |
| `changed_at` | timestamptz | no |  |  | default `now()` |

**Allowed values**

- `action` — `created`, `scopes_changed`, `revoked`, `reactivated`, `other`

> ⚠️ Row-level security is **not** enabled on this table.

### `access_tokens`

One table for every bearer token: per-user MCP tokens (/api/mcp) and per-user
API tokens (/api/resman). Hash-only — the plaintext is shown once at creation
and never stored. (Scanner keys are separate: see scanner_devices.)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `token_hash` | text | no | UQ |  | SHA-256 hex of the bearer token |
| `token_prefix` | text | no |  |  | first chars for identification (not secret) · default `''` |
| `kind` | text | no |  |  | default `'mcp'` |
| `subject_type` | text | no |  |  | default `'admin_user'` |
| `subject_id` | text | no |  | → _polymorphic, see above_ | admin_users.id or scanner_id · default `''` |
| `label` | text | no |  |  | display name (staff / scanner) · default `''` |
| `role` | text | no |  |  | default `'staff'` |
| `scopes` | text[] | no |  |  | resource-name allowlist; empty = all · default `'{}'[]` |
| `active` | boolean | no |  |  | default `true` |
| `last_used_at` | timestamptz | yes |  |  |  |
| `created_at` | timestamptz | no |  |  | default `now()` |
| `revoked_at` | timestamptz | yes |  |  |  |

**Referenced by** — [`access_token_audit_log.token_id`](#accesstokenauditlog) (FK)

**Allowed values**

- `kind` — `mcp`, `api_resman`
- `subject_type` — `admin_user`, `scanner`

## Preventive maintenance

Recurring maintenance templates and the per-unit task rounds generated from them.

### `pm_tasks`

pm_tasks — one row per (template, round, unit); generated nightly

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `template_id` | uuid | no | UQ | → FK [`pm_templates.id`](#pmtemplates) |  |
| `round_key` | text | no | UQ |  | "YYYY-MM" of the period start |
| `unit_number` | text | no | UQ |  |  |
| `due_date` | date | no |  |  |  |
| `status` | text | no |  |  | default `'pending'` |
| `completed_by` | text | no |  |  | default `''` |
| `completed_at` | timestamptz | yes |  |  |  |
| `resman_work_order_id` | text | yes |  | → ref [`resman_work_orders.resman_work_order_id`](#resmanworkorders) |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `status` — `pending`, `done`, `skipped`

### `pm_templates`

pm_templates — admin-defined recurring maintenance definitions

Emberly-owned preventive maintenance (PM). Admins define templates; the sync
worker expands active templates into per-unit task "rounds" nightly and
idempotently (pm_tasks). ResMan is never written.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `name` | text | no |  |  |  |
| `category` | text | no |  |  | default `''` |
| `cadence` | text | no |  |  |  |
| `anchor_month` | integer | yes |  |  |  |
| `scope_type` | text | no |  |  | default `'all'` |
| `scope_values` | text[] | no |  |  | default `'{}'[]` |
| `active` | boolean | no |  |  | default `true` |
| `created_by` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`pm_tasks.template_id`](#pmtasks) (FK)

**Allowed values**

- `scope_type` — `all`, `building`, `classification`
- `cadence` — `monthly`, `quarterly`, `semiannual`, `annual`

## Gate entry

Scanner records. Currently EMPTY in production — the scanners have never been used, so a zero here is not a quiet night.

### `entry_log_photos`

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `entry_log_id` | uuid | no |  | → FK [`entry_logs.id`](#entrylogs) |  |
| `resident_id` | uuid | yes |  | → FK [`residents.id`](#residents) |  |
| `guest_pass_id` | uuid | yes |  | → FK [`guest_passes.id`](#guestpasses) |  |
| `entry_type` | text | no |  |  |  |
| `scanner_id` | text | yes |  |  |  |
| `storage_path` | text | no |  |  |  |
| `content_type` | text | no |  |  |  |
| `byte_size` | integer | no |  |  |  |
| `flagged_at` | timestamptz | yes |  |  |  |
| `retention_expires_at` | timestamptz | no |  |  | default `(now() + '30 days')` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `entry_type` — `resident`, `guest`

### `entry_logs`

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resident_id` | uuid | yes |  | → FK [`residents.id`](#residents) |  |
| `guest_pass_id` | uuid | yes |  | → FK [`guest_passes.id`](#guestpasses) |  |
| `entry_type` | text | no |  |  |  |
| `tenant_name` | text | no |  |  |  |
| `unit_address` | text | no |  |  |  |
| `property_name` | text | no |  |  |  |
| `entered_at` | timestamptz | yes |  |  | default `now()` |
| `scanner_id` | text | yes |  |  |  |
| `notes` | text | yes |  |  |  |

**Referenced by** — [`entry_log_photos.entry_log_id`](#entrylogphotos) (FK)

**Allowed values**

- `entry_type` — `resident`, `guest`

## Residents

Resident identities and their enrolled devices.

### `resident_devices`

Resident device sessions

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resident_id` | uuid | no |  | → FK [`residents.id`](#residents) |  |
| `token_hash` | text | no | UQ |  |  |
| `user_agent` | text | yes |  |  |  |
| `active` | boolean | no |  |  | default `true` |
| `expires_at` | timestamptz | no |  |  |  |
| `last_seen_at` | timestamptz | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `resident_entry_token_uses`

Resident entry-token single-use ledger (see 20260724_resident_entry_token_uses.sql):
the resident QR carries a unique jti; verify-pass records it here on first scan,
so a screenshot/replay within its 60s TTL hits the primary-key conflict and is
rejected. Service-role only (RLS on, no policies); expires_at drives cleanup pruning.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `jti` | text | no | **PK** |  |  |
| `resident_id` | text | yes |  |  |  |
| `used_at` | timestamptz | no |  |  | default `now()` |
| `expires_at` | timestamptz | no |  |  |  |

### `residents`

Emberly Security — Supabase Schema

Enable UUID extension (already available in Supabase)
create extension if not exists "pgcrypto";

Residents (synced from Resman stub)

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_ledger_id` | text | no | UQ |  |  |
| `resman_login` | text | no |  |  | Resman portal username (shared across all residents in a household) |
| `name` | text | no |  |  |  |
| `unit_id` | text | no |  |  |  |
| `access_allowed` | boolean | no |  |  | default `false` |
| `access_status` | text | yes |  |  |  |
| `last_resman_verified_at` | timestamptz | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Referenced by** — [`entry_log_photos.resident_id`](#entrylogphotos) (FK), [`entry_logs.resident_id`](#entrylogs) (FK), [`guest_pass_bans.resident_id`](#guestpassbans) (FK), [`guest_passes.resident_id`](#guestpasses) (FK), [`resident_devices.resident_id`](#residentdevices) (FK)

## Everything else

### `delinquency_actions`

delinquency_actions — collections timeline for the manager app

Emberly-owned write surface: property managers record collection touchpoints
(calls, notices served, promises to pay, FED filings, write-offs, …) against
a lease from the manager app. The lease reference is soft — the sync's
delete-missing pass may remove a lease, but the action history must survive.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_lease_id` | text | no |  | → ref [`resman_leases.resman_lease_id`](#resmanleases) | soft ref (lease may be deleted by sync) |
| `resman_unit_id` | text | no |  | → ref [`resman_units.resman_unit_id`](#resmanunits) | default `''` |
| `unit_number` | text | no |  |  | default `''` |
| `kind` | text | no |  |  |  |
| `note` | text | no |  |  | default `''` |
| `amount` | numeric | yes |  |  | legal cost or promise amount, context-dependent |
| `promise_due_date` | date | yes |  |  | only for promise_recorded / payment_plan |
| `created_by` | text | no |  |  | staff display name from token label · default `''` |
| `created_by_admin_id` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_at` | timestamptz | yes |  |  |  |

**Allowed values**

- `kind` — `note`, `called`, `notice_served`, `promise_recorded`, `promise_kept`, `promise_broken`, `fed_filed`, `eviction_completed`, `writeoff`, `payment_plan`

### `insurance_actions`

insurance_actions — the insurance compliance follow-up trail

Emberly-owned write surface, the delinquency_actions pattern again: property
managers record proof requests, second notices and manual verifications
against a lease from the manager app's Compliance board. ResMan stays the
source of the policy record (resman_lease_insurance); Emberly owns what we
did about it. "Lapse detected" rows are NOT stored — lapse is derived from
the policy end date on device. The lease reference is soft — the sync's
delete-missing pass may remove a lease, but the follow-up history must
survive it.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_lease_id` | text | no |  | → ref [`resman_leases.resman_lease_id`](#resmanleases) | soft ref (lease may be deleted by sync) |
| `unit_number` | text | no |  |  | default `''` |
| `kind` | text | no |  |  |  |
| `note` | text | no |  |  | default `''` |
| `created_by` | text | no |  |  | staff display name from token label · default `''` |
| `created_by_admin_id` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_at` | timestamptz | yes |  |  |  |

**Allowed values**

- `kind` — `proof_requested`, `second_notice`, `verified`, `note`

### `lease_notes`

lease_notes — shared staff notes thread on a lease

Emberly-owned write surface, the delinquency_actions pattern again: any
staff role posts free-text notes against a lease from the manager app's
pipeline detail sheet; ResMan is never touched. The lease reference is soft
— the sync's delete-missing pass may remove a lease, but the conversation
must survive it — so rows are soft deleted (deleted_at) and never cascade.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_lease_id` | text | no |  | → ref [`resman_leases.resman_lease_id`](#resmanleases) | soft ref (lease may be deleted by sync) |
| `unit_number` | text | no |  |  | default `''` |
| `body` | text | no |  |  |  |
| `created_by` | text | no |  |  | staff display name from token label · default `''` |
| `created_by_role` | text | no |  |  | token role, shown next to the name · default `''` |
| `created_by_admin_id` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_at` | timestamptz | yes |  |  |  |

### `manager_alert_notifications`

manager_alert_notifications — the manager app's push-alert ledger.
One row per alert that has been SENT (or claimed for sending). The sync
worker's manager-alerts job inserts the row BEFORE calling Expo, so an
alert can fire at most once: (kind, subject_key) is unique and the insert
is the claim. `subject_key` is the source row's natural id per kind —
resman_lease_id (application_received / lease_signed), resman_unit_id
(balance_threshold), delinquency_actions.id (eviction_milestone),
mlgw_bills.id (utility_spike), renewal_offers.id (renewal_offer_silent), or
resman_lease_insurance.resman_insurance_id (policy_lapsed).
Only balance_threshold rows are ever removed: the job deletes them once a
unit's balance falls back under the re-arm band so a later climb over the
threshold can alert again. Every other kind's row is permanent.
Service-role only (RLS on, no policies).

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `kind` | text | no | UQ |  |  |
| `subject_key` | text | no | UQ |  | source row id; unique with kind |
| `unit_number` | text | no |  |  | default `''` |
| `title` | text | no |  |  | default `''` |
| `body` | text | no |  |  | PII-light copy as sent · default `''` |
| `amount` | numeric | yes |  |  |  |
| `devices` | integer | no |  |  | tokens the alert was addressed to · default `0` |
| `notified_at` | timestamptz | no |  |  | default `now()` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `kind` — `application_received`, `lease_signed`, `balance_threshold`, `eviction_milestone`, `utility_spike`, `renewal_offer_silent`, `policy_lapsed`

### `monitor_findings`

monitor_findings — what the monitor noticed, deduplicated

One row per DISTINCT finding, not per detection: `fingerprint` is unique, so
a condition that persists for a week updates last_seen_at instead of filing
seven rows. resolved_at is set when a finding stops reproducing, which is
what makes "still open" answerable. notified_at records that a digest went
out, so a restart cannot re-notify the same finding.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `fingerprint` | text | no | UQ |  |  |
| `kind` | text | no |  |  |  |
| `severity` | text | no |  |  |  |
| `resource` | text | no |  |  |  |
| `entity` | text | yes |  |  |  |
| `period` | text | yes |  |  |  |
| `summary` | text | no |  |  |  |
| `detail` | jsonb | yes |  |  |  |
| `first_seen_at` | timestamptz | no |  |  | default `now()` |
| `last_seen_at` | timestamptz | no |  |  | default `now()` |
| `resolved_at` | timestamptz | yes |  |  |  |
| `created_at` | timestamptz | no |  |  | default `now()` |
| `notified_at` | timestamptz | yes |  |  |  |

**Allowed values**

- `severity` — `info`, `warn`, `critical`
- `kind` — `anomaly`, `staleness`

> ⚠️ Row-level security is **not** enabled on this table.

### `property_snapshots`

property_snapshots — one row per property per day, written by the sync
worker's nightly snapshots step after the mirror jobs run. The Trends
charts read this table directly; no per-request aggregation.
ALL metric columns are nullable ON PURPOSE: the one-shot backfill
reconstructs only what lease spans can prove (the occupancy family) and
leaves every other column null, so a null reads as "series not yet begun"
and the charts label the series start instead of faking a flat past.
`source` records which writer produced the row: 'nightly' (the daily job;
a same-day re-run overwrites) or 'backfill' (the lease-span reconstruction;
it never overwrites an existing row).
Service-role only (RLS on, no policies).

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `snapshot_date` | date | no | **PK** |  |  |
| `total_units` | integer | yes |  |  |  |
| `occupied_units` | integer | yes |  |  |  |
| `vacant_units` | integer | yes |  |  |  |
| `occupancy_pct` | numeric | yes |  |  |  |
| `rent_roll` | numeric | yes |  |  |  |
| `lease_rent_total` | numeric | yes |  |  |  |
| `balance_total` | numeric | yes |  |  |  |
| `balance_0_30` | numeric | yes |  |  |  |
| `balance_31_60` | numeric | yes |  |  |  |
| `balance_61_90` | numeric | yes |  |  |  |
| `balance_90_plus` | numeric | yes |  |  |  |
| `delinquent_units` | integer | yes |  |  |  |
| `turns_in_progress` | integer | yes |  |  |  |
| `open_work_orders` | integer | yes |  |  |  |
| `utility_due` | numeric | yes |  |  |  |
| `source` | text | no |  |  | default `'nightly'` |
| `created_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `source` — `nightly`, `backfill`

### `push_tokens`

Expo push tokens for the staff apps (see deltas/2026-07-21-push-tokens.sql):
the maintenance app registers its device token via POST /api/admin/push-tokens
and the sync worker fans emergency work-order alerts out to every active row.
`app` is the fleet discriminator ('maintenance' | 'manager') — every sender
MUST filter on it so one app's alerts never reach the other's devices.
Service-role only (RLS on, no policies), like the rest of the first-party tables.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `expo_push_token` | text | no | UQ |  | "ExponentPushToken[...]"; opaque here |
| `admin_id` | text | no |  |  | staff subject the app authenticated as |
| `display_name` | text | no |  |  | default `''` |
| `platform` | text | no |  |  | default `'ios'` |
| `app` | text | no |  |  | default `'maintenance'` |
| `alert_kinds` | text[] | no |  |  | default `'{}'[]` |
| `active` | boolean | no |  |  | default `true` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |
| `last_seen_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `platform` — `ios`, `android`

### `rate_limits`

Durable rate limiting

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `bucket` | text | no | **PK** |  |  |
| `window_start` | timestamptz | no |  |  |  |
| `count` | integer | no |  |  | default `0` |
| `expires_at` | timestamptz | no |  |  |  |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `renewal_offers`

renewal_offers — renewal pipeline for the manager app

Emberly-owned write surface, the delinquency_actions pattern again: every
expiring lease moves through a tracked offer flow (needs offer → offer sent
→ response). ResMan stays the lease of record; Emberly owns the offer
workflow. The lease reference is soft — the sync's delete-missing pass may
remove a lease, but the offer history (and the lift metric it feeds) must
survive. term_months null + is_month_to_month true is the MTM offer; the
MTM premium is just the proposed_rent.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_lease_id` | text | no |  | → ref [`resman_leases.resman_lease_id`](#resmanleases) | soft ref (lease may be deleted by sync) |
| `resman_unit_id` | text | no |  | → ref [`resman_units.resman_unit_id`](#resmanunits) | default `''` |
| `unit_number` | text | no |  |  | default `''` |
| `prior_rent` | numeric | yes |  |  | resident rent when the offer went out |
| `proposed_rent` | numeric | no |  |  |  |
| `term_months` | integer | yes |  |  | null for month-to-month offers |
| `is_month_to_month` | boolean | no |  |  | default `false` |
| `status` | text | no |  |  | default `'sent'` |
| `sent_at` | timestamptz | yes |  |  | default `now()` |
| `responded_at` | timestamptz | yes |  |  | stamped when the offer resolves |
| `note` | text | no |  |  | default `''` |
| `created_by` | text | no |  |  | staff display name from token label · default `''` |
| `created_by_admin_id` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_at` | timestamptz | yes |  |  |  |

**Allowed values**

- `status` — `sent`, `accepted`, `declined`, `withdrawn`

### `scanner_devices`

Allow service role full access (used by API routes)
Public/anon access is denied by default with RLS enabled and no policies

Admin: scanner device registry

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `scanner_id` | text | no | UQ |  |  |
| `name` | text | no |  |  |  |
| `location` | text | yes |  |  |  |
| `enabled` | boolean | no |  |  | default `true` |
| `secret_hash` | text | yes |  |  |  |
| `secret_rotated_at` | timestamptz | yes |  |  |  |
| `last_seen_at` | timestamptz | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

### `unit_snapshots`

Flattened from lib/supabase/deltas on 2026-08-01

Everything below reached production through a dated delta and is folded in
here as its end state, per the convention in migrations/README.md: this file
provisions a fresh database on its own, and `deltas/` starts empty again.
Superseded revisions are NOT reproduced. mcp_aggregate was defined four
times and mcp_predicate three as their capabilities grew; only the final
definition of each appears here. Replaying the deltas left the earlier
overloads behind in the database — `create or replace function` with a new
signature creates a new function rather than replacing the old one — and a
fresh database built from this file simply never has them.

unit_snapshots — per-unit daily history

resman_units is a MIRROR: the sync overwrites it with current state, so
yesterday's occupancy is gone the moment it runs. This table is the only
record of what a unit looked like on a given day, written nightly by
supabase/sync/src/run-unit-snapshots.ts. Keyed (snapshot_date, unit) so a
re-run of the same night is an upsert, not a duplicate.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `snapshot_date` | date | no | **PK** |  |  |
| `resman_unit_id` | uuid | no | **PK** |  |  |
| `unit_number` | text | yes |  |  |  |
| `resman_building_id` | uuid | yes |  | → ref [`resman_buildings.resman_building_id`](#resmanbuildings) |  |
| `resman_floorplan_id` | uuid | yes |  | → ref [`resman_floorplans.resman_floorplan_id`](#resmanfloorplans) |  |
| `occupancy_status` | text | yes |  |  |  |
| `occupied` | boolean | yes |  |  |  |
| `lease_status` | text | yes |  |  |  |
| `availability` | text | yes |  |  |  |
| `balance` | numeric | yes |  |  |  |
| `current_month_balance` | numeric | yes |  |  |  |
| `market_rent` | numeric | yes |  |  |  |
| `lease_rent` | numeric | yes |  |  |  |
| `times_late` | integer | yes |  |  |  |
| `holding_unit` | boolean | yes |  |  |  |
| `excluded_from_occupancy` | boolean | yes |  |  |  |
| `move_in_date` | date | yes |  |  |  |
| `move_out_date` | date | yes |  |  |  |
| `lease_end_date` | date | yes |  |  |  |
| `source` | text | no |  |  | default `'nightly'` |
| `created_at` | timestamptz | no |  |  | default `now()` |

> ⚠️ Row-level security is **not** enabled on this table.

### `unit_tags`

Shared, expiring unit tags (see migrations/20260722_unit_tags.sql). Ports the
Swift UnitTagStore into the synced model; expiration rules run server-side.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `unit_number` | text | no |  |  |  |
| `label` | text | no |  |  |  |
| `color_hex` | text | no |  |  | default `'#5B7C99'` |
| `expiry_kind` | text | no |  |  | default `'never'` |
| `expires_at` | timestamptz | yes |  |  |  |
| `bound_lease_id` | text | yes |  |  |  |
| `status_trigger` | text | yes |  |  |  |
| `origin` | text | no |  |  | default `'admin'` |
| `created_by_display_name` | text | yes |  |  |  |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `updated_at` | timestamptz | yes |  |  | default `now()` |

**Allowed values**

- `origin` — `admin`, `scanner`
- `expiry_kind` — `never`, `date`, `duration`, `move_out`, `status_change`

### `work_order_photos`

work_order_photos — completion photos attached by maintenance techs

Technicians attach before/after photos when closing a work order in the
maintenance app. ResMan write-back is deferred (the close route is a stub),
so bytes live in the private `work-order-photos` Storage bucket and these
rows carry the pointer + author; the photos ride into ResMan when the
deferred write path is built. The FK cascades because the sync's
property-scoped delete-missing pass removes work orders that leave the
ResMan report.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `id` | uuid | no | **PK** |  | default `gen_random_uuid()` |
| `resman_work_order_id` | text | no |  | → FK [`resman_work_orders.resman_work_order_id`](#resmanworkorders) |  |
| `phase` | text | no |  |  | default `'completion'` |
| `storage_path` | text | no |  |  |  |
| `content_type` | text | no |  |  |  |
| `byte_size` | integer | no |  |  |  |
| `created_by` | text | no |  |  | staff display name · default `''` |
| `created_by_admin_id` | text | no |  |  | default `''` |
| `created_at` | timestamptz | yes |  |  | default `now()` |
| `deleted_at` | timestamptz | yes |  |  |  |

**Allowed values**

- `phase` — `before`, `after`, `completion`

### `work_order_translations`

work_order_translations — server-side translation cache for work-order prose

The sync worker translates title/notes/completion_notes via Langbly and
stores the result keyed by a content hash of the SOURCE text
(packages/core textHash) plus the target language. The maintenance app reads
this and merges it into its on-device cache under the identical
`${lang}:${hash}` key, so a phone never re-translates prose the server
already paid to translate. Content-addressed: when a work order's text
changes its hash changes, a new row is written, and the stale row is reaped
once no live work order hashes to it.

| column | type | null | key | references | notes |
| --- | --- | --- | --- | --- | --- |
| `source_hash` | text | no | **PK** |  |  |
| `target_lang` | text | no | **PK** |  |  |
| `source_lang` | text | no |  |  |  |
| `translated_text` | text | no |  |  |  |
| `char_count` | integer | no |  |  | default `0` |
| `updated_at` | timestamptz | no |  |  | default `now()` |

**Allowed values**

- `target_lang` — `en`, `es`

---

_Regenerate with `bun run docs:schema`._
