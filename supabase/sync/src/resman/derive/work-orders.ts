/**
 * WorkOrderTagging + WorkOrderDuplicateDetector — pure functions (tag keyword
 * table, token weights, possible-callback-v2 engine). Run as a post-upsert
 * derive step over resman_work_orders. Design §3.1. Milestone 3.
 */
export {};
